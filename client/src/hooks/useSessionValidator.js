/**
 * useSessionValidator — no-op.
 *
 * The dining session token system has been removed. QR scanning now only
 * sets the table number; no session token is created or validated.
 * This stub is kept so any remaining imports don't cause build errors.
 */
export function useSessionValidator() {
  // intentionally empty
}
